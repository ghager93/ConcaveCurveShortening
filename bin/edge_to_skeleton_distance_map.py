import numpy as np

import line_drawing

from .util.vector2d import Vector2D

from .morphology import skeleton_mapping

ea = np.zeros((100, 100))
sk = np.zeros((100, 100))
da = np.zeros((100, 100))

d = dict()

